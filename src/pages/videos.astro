---
import SiteLayout from "../layouts/SiteLayout.astro";
import { getVideos } from "../lib/content";

const videos = getVideos();
---

<SiteLayout title="视频记录" description="Evan 的游戏开发原型、技术验证与视频记录。" current="video" bodyClass="plain-content-page" interfaceStyle="ornate">
  <main class="site-shell">
    <section class="page-intro">
      <p class="eyebrow">02 / BUILD CAPTURE</p>
      <h1 class="pixel-title">视频记录</h1>
    </section>
    <section class="switch-player-list archive-list" aria-label="视频列表">
      {
        videos.length ? (
          videos.map((video) => (
            <article class="switch-player" id={video.elementId}>
              <div class="switch-player-body">
                <div class="switch-player-copy"><h3>{video.title}</h3></div>
                <div class="switch-screen">
                  <iframe
                    src={video.embedUrl}
                    title={video.title}
                    allow="autoplay; fullscreen; picture-in-picture"
                    referrerpolicy="strict-origin-when-cross-origin"
                    loading="lazy"
                    allowfullscreen
                  />
                </div>
              </div>
            </article>
          ))
        ) : (
          <p class="empty-state">暂时还没有视频。</p>
        )
      }
    </section>
  </main>
</SiteLayout>
